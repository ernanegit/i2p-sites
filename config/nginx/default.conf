# Blog PHP
server {
    listen 80;
    server_name blog.i2p localhost;
    
    location / {
        proxy_pass http://blog-php:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-I2P-Network "true";
    }
}

# Chat Node.js  
server {
    listen 80;
    server_name chat.i2p;
    
    location / {
        proxy_pass http://chat-nodejs:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Default server
server {
    listen 80 default_server;
    server_name _;
    
    return 200 '🔗 I2P Network Simulator - Acesse: blog.i2p ou chat.i2p';
    add_header Content-Type text/plain;
}